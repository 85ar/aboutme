import React from "react";
import { Layout } from "antd";
import "./styles.scss";

const Footer = () => {
  const { Footer } = Layout;

  return (
    <div>
      <Layout>
        <Footer>Footer</Footer>
      </Layout>
    </div>
  );
};

export default Footer;
